import { css } from 'styled-components';

const sunPositioning = css`
  position: absolute;
  align-self: center;
  margin-left: -25vmin;
  width: 50vmin;
  height: 50vmin;
  border-radius: 50%;
`;

export default sunPositioning;
